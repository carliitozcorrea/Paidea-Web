<div class="content-box-large">
    <div class="panel-heading">
        <div class="panel-title">T Perfil</div>
        <div class="panel-options">
            <a href="{{path ('adminhome') }}"class="btn btn-primary"><i class="glyphicon glyphicon glyphicon-chevron-left"></i> T Regresar</a>
        </div>
        <div class="panel-body">
            <div class="form-group">
                <label>{{ 'profile.show.username'|trans({}, 'FOSUserBundle') }}</label>
                <span class="form-control"> {{ user.username }}</span>
            </div>
            <div class="form-group">
                <label>{{ 'profile.show.email'|trans({}, 'FOSUserBundle') }}</label>
                <span class="form-control"> {{ user.email }}</span>
            </div>
            <div class="form-group">
                <label>T Nombre</label>
                <span class="form-control"> {{ user.name }}</span>
            </div>
            <a href="{{path ('fos_user_change_password') }}"class="btn btn-info">T Cambiar Contraseña</a>
            <a href="{{path ('fos_user_profile_edit') }}"class="btn btn-info">T Editar</a>

        </div>
    </div>
</div>